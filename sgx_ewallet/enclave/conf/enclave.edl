/* Enclave.edl - Top EDL file. */

enclave {

    untrusted {
        void ocall_print_string([in, string] const char *str);
    };


    trusted {
         public int ecall_print_prime_random();
    };
};
